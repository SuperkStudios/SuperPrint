import { OperatorConsole } from "@/components/operator-console";

export const metadata = {
  title: "Operator Console - SuperPrint OS"
};

export default function OperatorPage() {
  return <OperatorConsole />;
}
